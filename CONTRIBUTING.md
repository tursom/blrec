# 贡献指南

感谢你帮助维护 blrec。本仓库接受缺陷报告、功能建议、文档改进和代码贡献。

## 选择协作入口

- 可复现的缺陷和明确的功能需求提交到 [Issues](https://github.com/tursom/blrec/issues)。
- 使用问题、部署交流和设计讨论发布到 [Discussions](https://github.com/tursom/blrec/discussions)。
- 已有明确问题和验证方式的改动可以提交 Pull Request。

提交前先搜索现有 Issue、Discussion 和 Pull Request，避免重复工作。较大的功能或跨模块改动应先讨论范围和兼容性。

## 报告缺陷

缺陷报告至少应包含：

- blrec 版本或 commit
- 操作系统、CPU 架构和安装方式
- 使用的流格式、录制模式及相关设置
- 可复现的操作步骤
- 预期行为和实际行为
- 对应时间段的错误信息或日志

请把日志作为文本或附件提供，不要只提交无法搜索的截图。录制问题应说明直播间、发生时间和输出文件类型；无法公开直播间时，可以使用脱敏后的标识。

公开内容前必须移除 Cookie、API Key、Webhook URL、邮箱凭据、代理凭据和其他账号信息。无法安全脱敏的材料不要提交到公开 Issue。

## 提出功能需求

请先描述要解决的实际问题、当前工作方式和可接受的结果，再说明建议方案。功能请求需要明确是否影响已有设置、API、文件格式或部署方式。

## 开发准备

环境配置、运行、构建和测试命令见[开发指南](docs/development.md)。架构与跨模块边界见[项目架构](docs/architecture.md)。

开始修改前，请确认工作基于最新的 `master`，并把无关改动留在 Pull Request 之外。

## Pull Request 要求

Pull Request 应当：

- 说明问题、解决方式和行为变化
- 链接对应的 Issue 或 Discussion
- 保持改动范围聚焦，不顺带重构无关代码
- 为新增或修复的行为补充适当测试
- 更新受影响的用户文档、开发文档和更新日志
- 列出实际执行的验证命令及结果

修改 REST API、WebSocket 事件或设置模型时，需要同步检查后端 schema、Angular 模型和调用方。修改前端后，应从 `webapp/src` 重新构建嵌入式静态资源。

不要手工编辑 `src/blrec/data/webapp` 下的生成文件。不要在 Pull Request 中提交本地设置、日志、录播文件、虚拟环境或包含凭据的调试材料。

## 文档规范

主文档使用简体中文，命令、路径、配置项和代码标识使用反引号。先说明前置条件，再给操作步骤，最后描述验证和失败处理。

文档中的发行状态必须与 GitHub Releases、GHCR 和工作流保持一致。尚未发布的产物应明确标注为计划内容，不能写成当前可用。
