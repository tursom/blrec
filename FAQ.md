# 常见问题

## 如何终止程序？

在运行 blrec 的终端中按 `Ctrl+C`，等待程序保存设置并完成退出。

## 设置修改后什么时候生效？

大部分设置会立即生效，但正在录制的文件存在例外。

保存位置或路径模板只会影响下一次创建的文件。画质设置会在下一次获取直播流 URL 时生效；需要立即切换时，可以停止录制后重新开始。

## 什么是流拼接和无缝拼接？

流拼接是在录制连接中断后，从恢复位置继续写入原录播文件，避免一次直播产生过多片段。

无缝拼接会识别并丢弃中断前后重复的数据，再连接两段流。只有短时间内恢复连接，且前后数据存在可识别的重叠时，才可能完成无缝拼接。

## 如何确认是否出现流中断？

流中断会记录在日志中，详细信息也会写入视频文件元数据的 `Comment` 字段。可以使用 `ffprobe` 查看：

```bash
ffprobe -show_entries format_tags=Comment -of default=nw=1 recording.flv
```

VLC 可以通过媒体信息查看注释，PotPlayer 可以通过文件信息查看评论。MP4 还会在非无缝拼接位置添加章节标记，支持章节的播放器可以直接定位。

## 如何降低流中断和漏录概率？

CPU 长时间满载、网络带宽不足或抖动、磁盘写入过慢都会导致录制连接中断。应为同时录制的任务预留足够的 CPU、网络和磁盘吞吐，并关注日志中的重连信息。

主播网络或 B 站服务端导致的中断无法由录制端消除。连接未能及时恢复时，也可能没有足够的重复数据完成无缝拼接。

## 为什么录播文件的时长小于直播持续时间？

常见原因包括开播到推流之间存在延迟、录制中断未能无缝拼接，以及原始流时间戳发生跳变或反跳。blrec 修正异常时间戳后，媒体时长也可能缩短。

## 为什么实际录制画质与设置不一致？

设置的画质在当前直播中不一定可用。目标画质不存在时，程序会按现有逻辑选择可用流，实际结果可能与设置名称不同。

## 为什么分割后的文件小于设置的大小或时长？

为避免丢帧，文件只能在合适的关键帧处分割。blrec 会选择超过限制前的关键帧，因此实际文件通常会略小或略短。

## 为什么没有设置分割仍产生多个文件？

主播修改分辨率、码率等流参数时，blrec 会自动分割文件以降低花屏和解码异常风险。主播短时间内多次下播、开播也会产生多个文件。

## 为什么启用 MP4 转换后没有执行？

确认 `ffmpeg` 已正确安装，并且运行 blrec 的用户可以在同一环境中执行：

```bash
ffmpeg -version
```

如果使用容器，请同时检查后处理日志、输出目录权限和磁盘空间。

HLS 转封装会先尝试移除无效填充数据，失败后自动使用普通封装复制重试。两次都失败时，程序会保留原始 `.m4s`、`.m3u8` 和元数据，便于排查或手动处理。

## 程序异常退出后，未完成的 HLS 录制会怎样？

下次启动时，blrec 会在已加载任务的输出目录中检查缺少结束标记的 HLS 录制。如果 `.m4s`、playlist 和房间元数据能够对应，程序会保留 playlist 中最长的有效分片前缀并补上结束标记，再按该任务的后处理设置继续处理。

恢复过程不会截断或改写 `.m4s`。没有有效分片、元数据不匹配或 playlist 无法解析时，原文件会保持不变，并在日志中记录原因。

## 哪些文件会被视为旧录播？

修改时间和访问时间都超过 24 小时的文件，才会在空间不足时进入自动回收范围。

## 空间不足时如何删除旧文件？

程序根据修改时间和访问时间选择较早的旧文件，持续删除，直到可用空间达到设置的阈值。启用自动回收前，应确认录播目录和阈值配置符合数据保留要求。

## 支持录制付费直播吗？

项目不承诺支持付费直播。可以在网络请求设置中配置已授权账号的 Cookie 进行验证，但 B 站接口和权限策略变化可能导致录制失败。

不要在 Issue、Discussion、日志或截图中公开 Cookie。提交问题时只保留与错误有关的脱敏信息。

## 如何导出 B 站请求历史用于排错？

请求历史默认启用，保存在日志目录下的 `http-history` 子目录。默认保留 7 天且最多占用 100 MiB；可以在设置页的“HTTP 请求历史”区域修改开关、保留时间和空间上限。

发生 B 站 API、WBI、播放地址或流连接问题后，在同一区域选择直播间和时间范围，然后下载 ZIP 排障包。排障包包含版本信息、筛选条件和按时间排序的 `records.jsonl`，不包含持续媒体正文。

Cookie、授权请求头、API Key、WBI 签名和播放地址凭证会在落盘前自动移除。自动脱敏不能替代人工确认；把 ZIP 上传到公开 Issue 前，仍应检查其中是否包含不希望公开的直播间、接口响应或其他信息。

## blrec 与直接下载直播流有什么区别？

blrec 不只负责下载数据，还处理直播监控、弹幕采集、流参数变化、时间戳修复、中断拼接、文件分割、后处理和长期运行时的磁盘治理。

这些能力用于降低直播不稳定时的文件损坏、漏录和过度分片风险，但不能消除主播网络或平台服务端造成的数据缺失。
